describe 'JWT', ()->

    beforeEach ()->
        return # TODO: Require jwt module and create mocks

    describe 'sign', ()->
        it 'should be a function', ()->
            return

    describe 'verify', ()->
        it 'should be a function', ()->
            return
